package com.backend134.salon.repositories;

import com.backend134.salon.models.GalleryText;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GalleryTextRepository extends JpaRepository<GalleryText, Long> {
}
